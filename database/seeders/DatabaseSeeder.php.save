<?php

namespace Database\Seeders;

use App\Models\User;
use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    use WithoutModelEvents;

    /**
     * Seed the application's database.
     */
    public function run(): void
    {
        // ១. បង្កើត User តែបើមិនមានទេ
        User::firstOrCreate(
            ['email' => 'lyhourheoun04@gmail.com'],
            [
                'name' => 'Lyhuo',
                'password' => bcrypt('password'),
            ]
        );

        // ២. ហៅ Seeder ផ្សេងៗទៀត (ហៅតែម្តងគត់ក្នុងម្នាក់)
        $this->call([
        TeacherSeeder::class,
        StudentSeeder::class,
        SchoolClassSeeder::class, // add this

        ]);


                $this->call([
            TeacherSeeder::class,
            StudentSeeder::class,
            SchoolClassSeeder::class,
            CourseSeeder::class,
            ShopSeeder::class,
$this->call([
    TeacherSeeder::class,
    StudentSeeder::class,
    SchoolClassSeeder::class,
    CourseSeeder::class,

    ShopSeeder::class,
    ProductSeeder::class,
    BookSeeder::class,

